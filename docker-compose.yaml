version: '3'
services:
  ubuntu-androidbuild:
    container_name: ubuntu-androidbuild
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
      - ./workspace:/workspace
      - ./tmp:/tmp
    network_mode: "bridge"
    privileged: true
    security_opt:
      - seccomp:unconfined
    tmpfs:
      - /run
      - /run/lock
    hostname: ubuntu-androidbuild
    cap_add:
      - ALL
